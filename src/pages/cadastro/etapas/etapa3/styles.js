import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    content: {
        marginBottom: 30,
    },
    title: {
        marginBottom: 10
    },
    inputGrid: {
        alignItems: 'center',
        flexDirection: 'row',
        justifyContent: 'center',
    },
    input: {
        width: '48%',
        marginHorizontal: 5,
    }
})